set terminal qt

f(x) = (x == 0 ? 1/0 : sin(2*x)/(3*x))
g(x) = sin(2*x)
h(x) = (x == 0 ? 1/0 : 1/x)

set title "f(x) = sin(2*x)/(3*x), near x = 0"
set xlabel "x"
set ylabel "f(x)"
set xrange [ -1 : 1]
#set yrange [107.9 : 108.1]
#set samples 5000
set grid

#plot f(x) with lines lw 2 lc rgb "red" title "f(x) = sin(2*x)/(3*x), near x = 0", \
 #   g(x) with lines lw 1 dt 2 lc rgb "green", \
  #  h(x) with lines lw 1 dt 2 lc rgb "green"

#plot g(x) with lines lw 1 dt 2 lc rgb "red"
plot h(x) with lines lw 1 dt 2 lc rgb "green"

pause -1